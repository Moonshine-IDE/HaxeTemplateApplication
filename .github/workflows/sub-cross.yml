name: SUB - Build Cross

on:    
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: crazymax/osxcross
    steps:

    - name: Build info
      run: |
        echo "Ref: ${{ github.ref_name }}"